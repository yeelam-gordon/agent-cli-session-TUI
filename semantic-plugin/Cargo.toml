[package]
name = "semantic-search-plugin"
version = "0.2.0"
edition = "2021"
description = "Optional semantic search plugin for agent-session-tui"
license = "MIT"

[lib]
crate-type = ["cdylib"]

[dependencies]
fastembed = "5"
serde_json = "1"
serde = { version = "1", features = ["derive"] }
